# 工作記錄 2026-07-29（週三）

## 建立 00-NB 工作區規則與收工流程

### 做了什麼
- 建立工作區規則檔 `CLAUDE.md`，明訂四條規則：一律繁體中文、每次做事結束前寫當日工作記錄、「收工」時彙整待辦、與 WSA 工作區／分支／GitHub 帳號完全分離
- 建立 `收工` skill（專案層級），涵蓋：補記錄 → 掃未辦事項 → 更新待辦清單 → 確認帳號後才問要不要 commit/push
- 建立工作記錄資料夾 `00_工作記錄\`，命名規則 `工作記錄_YYYYMMDD.md`，一天一檔
- `git init` 初始化 00-NB 為獨立 repo，設定 **repo-local** git 身分為 `Doris Chen <doris.chen2323@gmail.com>`
- 建立 `.gitignore`，把 `WSA/` 整個排除（另含憑證、OS、編輯器、Python/Node 慣例忽略）

### 產出／異動檔案
- `C:\Doris\00-NB\CLAUDE.md` — 新建，工作區四大規則
- `C:\Doris\00-NB\.claude\skills\收工\SKILL.md` — 新建，收工流程 skill
- `C:\Doris\00-NB\.gitignore` — 新建，核心是排除 `WSA/`
- `C:\Doris\00-NB\00_工作記錄\待辦事項.md` — 新建，滾動式待辦總清單
- `C:\Doris\00-NB\00_工作記錄\工作記錄_20260729.md` — 本檔
- `C:\Doris\00-NB\.git\` — 新建 repo，local user.email = doris.chen2323@gmail.com

### 決策與原因
- **git 身分設 repo-local 而非改全域**：全域仍是 `doris@waterballsa.tw`（WSA 用）。只在 00-NB 設 local，兩邊互不影響，也不會誤改到 WSA 的 commit 作者。
- **`WSA/` 用 .gitignore 排除、暫不搬移**：`C:\Doris\00-NB\WSA\aixbdd` 是 Waterball 的獨立 repo（有自己的 .git）。搬動資料夾風險較高，先用忽略規則達成「版控層面完全分離」，實體搬移列入待辦由使用者決定。
- **收工 skill 放專案層級 `.claude\skills\`**：既有的 `今日工作` skill 是 user-level 且綁 AI x BDD 路徑，會在任何專案觸發。收工流程綁 00-NB 路徑慣例，放專案層級避免污染其他工作區；skill 內另加 cwd 檢查作為防呆。
- **push 前強制檢查 remote 與 user.email**：Windows Git Credential Manager 會快取帳號，容易推錯帳號，故在 skill 步驟 5 設為硬性檢查點。

## 17:35 — 補上 GitHub 帳號與 skill 命名修正

### 做了什麼
- 取得個人 GitHub 帳號 `dorischen2323-boop`，寫入 `CLAUDE.md` 第 4 節；設 repo-local `credential.username = dorischen2323-boop`
- 查核發現 **`gh` CLI 登入的是 `Doris2323`**（第三個身分），與本 repo 帳號不同 → 在 CLAUDE.md 與 skill 中明訂「本 repo 不可用 `gh` 操作」
- 確認 `dorischen2323-boop` 帳號存在但 0 個 repo → `00-NB` repo 尚未建立，改由 Doris 手動建
- skill 因命名規則（只能小寫英數與連字號）由 `收工` 改名 `end-of-day`，觸發詞「收工」不受影響
- 預設分支由 `master` 改為 `main`
- 5 個檔案已 `git add`，確認 `WSA/` 完全未被追蹤；依規則等 remote 設好再一起 commit

### 產出／異動檔案
- `CLAUDE.md` — 補 GitHub 帳號、gh 帳號警告、skill 路徑更新
- `.claude\skills\end-of-day\SKILL.md` — 改名（原 `收工`）、移除不支援的 `allowed-tools` 欄位、補 gh 帳號警告
- `00_工作記錄\待辦事項.md` — 補建 repo 步驟與帳號注意事項

### 決策與原因
- **不用 `gh repo create` 代建 repo**：gh 登入的是 `Doris2323`，會建到錯的帳號底下。改請 Doris 手動建，避免帳號污染。
- **skill 改英文名**：VS Code 代理程式對 skill 名稱有 `^[a-z0-9-]+$` 限制，中文名會報錯。改名後靠 description 的中文觸發詞照樣可用「收工」二字啟動。

### 未完成／下一步
- [x] 已完成：接 remote → 首次 commit → push（見下節）
- [ ] `WSA\` 是否實體搬到 `C:\Doris\WSA\` 待決定

## 17:50 — 接上 GitHub remote 並完成首次 push

### 做了什麼
- Doris 手動建立 repo **`dorischen2323-boop/new_bu`**（Private、空 repo）
- `git remote add origin https://github.com/dorischen2323-boop/new_bu.git`
- 首次 commit `a58032b`（root commit，5 檔 / 368 行）
- push 首次因無快取憑證失敗（Claude 端無法彈出登入視窗）→ 由 Doris 在 session 內執行 `! git push` 完成登入與推送
- 驗證：`git ls-remote origin` 回 `a58032b refs/heads/main`，本機 `main` 已 track `origin/main`

### 產出／異動檔案
- `.git/config` — remote origin + upstream tracking
- GitHub `dorischen2323-boop/new_bu` — 首次 commit 已上線

### 決策與原因
- **repo 名稱用 `new_bu` 而非 `00-NB`**：Doris 實際建立的名稱為 `new_bu`，直接照實接上，未擅自要求改名。若日後改名，需同步更新本機 `git remote set-url`。
- **push 由 Doris 手動執行**：Claude 端 shell 為非互動模式，GCM 無法彈窗。憑證登入這類需要真人操作的步驟一律交回使用者，不嘗試繞過。

### 未完成／下一步
- [ ] `WSA\` 是否實體搬到 `C:\Doris\WSA\` 待決定

## 📌 明日待辦
- [ ] 決定 `WSA\` 是否搬出 00-NB（目前僅用 .gitignore 做版控隔離）
- [ ] 視需要決定 repo 是否從 `new_bu` 改名為 `00-NB`
